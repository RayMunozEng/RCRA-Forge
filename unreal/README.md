# Unreal fur source checkpoint

UE 5.8 experimental fur-authoring plugin and validation source, copied from the outer gem-shader workspace on 2026-09-08. SOURCE_CHECKPOINT.json records source-byte hashes. The working development copy remains in that outer workspace.

This is not a ready-to-install binary package. Game models, textures, extracted shader binaries, captures, generated Unreal content, built modules and caches are intentionally absent. Validation scripts need their original private fixtures and some retain original workspace paths. Building the plugin requires Unreal Engine 5.8; creating demo content requires the provided authoring scripts and appropriate user-owned inputs.

Dry fur is NOT at 1:1 parity. The ear contour remains unresolved. See fur/EAR_CHECKPOINT_20260908.md for the latest signed-facing correction, native strand-pipeline discovery, evidence limits, and the disk-space block on the next replay. The strand draws have not yet been attributed to Ratchet's ear.
